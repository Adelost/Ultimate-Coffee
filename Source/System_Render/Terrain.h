#pragma once

#include <vector>
#include <Core/Math.h>
#include "Vertex.h"

class Terrain
{
private:
	Int2 size;

public:
	Terrain()
	{
		//std::vector<Vertex::PosCol> patchVertices(num_patchVertex_total);
	}

	void buildQuadPatch()
	{
		//std::vector<Vertex::PosCol> patchVertices(num_patchVertex_total);
	}
};
